<div class='appWrapper'>
    <div class='earnings'><%= @ticket.sum %>₽ (<%= @ticket.share + @ticket.normal + @ticket.shareDrink + @ticket.normalDrink + @ticket.discount + @ticket.upgrade + @ticket.free %>)</div>
    <div class='ticketsWrapper'>
        <div class='ticket'>
            <span class='price'>75₽ (<%= @ticket.share%>)</span>
            <%= link_to '+', ticket_bought_path(@ticket, :price => 75), class:'ticketBought'%>
            <%= link_to '-', ticket_mistake_path(@ticket, :price => 75), class:'ticketMistake'%>
        </div>
        <div class='ticket'>
            <span class='price'>150₽ (<%= @ticket.normal%>)</span>
            <%= link_to '+', ticket_bought_path(@ticket, :price => 150), class:'ticketBought'%>
            <%= link_to '-', ticket_mistake_path(@ticket, :price => 150), class:'ticketMistake'%>
        </div>
        <div class='ticket'>
            <span class='price'>200₽ (<%= @ticket.normalDrink%>)</span>
            <%= link_to '+', ticket_bought_path(@ticket, :price => 20), class:'ticketBought'%>
            <%= link_to '-', ticket_mistake_path(@ticket, :price => 20), class:'ticketMistake'%>
        </div>
        <div class='ticket'>
            <span class='price'>1000₽ (<%= @ticket.shareDrink%>)</span>
            <%= link_to '+', ticket_bought_path(@ticket, :price => 1000), class:'ticketBought'%>
            <%= link_to '-', ticket_mistake_path(@ticket, :price => 1000), class:'ticketMistake'%>
        </div>
    </div>
<div>